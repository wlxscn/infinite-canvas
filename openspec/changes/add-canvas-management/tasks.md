## 1. 共享合同与数据模型

- [x] 1.1 在 `packages/shared/src/api.ts` 增加 `ProjectSummary`、项目列表、项目创建和项目重命名相关 API 合同。
- [x] 1.2 为前端项目管理状态定义清晰的本地类型，区分“项目摘要”和“完整 `CanvasProject` 快照”。
- [x] 1.3 明确默认项目标题、最近打开时间和项目切换动作需要重置的运行时状态边界。

## 2. 后端项目管理接口

- [x] 2.1 在 `apps/agent-api` 中新增项目列表接口，返回项目摘要列表并按最近更新时间排序。
- [x] 2.2 新增项目创建接口，为新画布生成项目记录并返回空白项目快照及摘要信息。
- [x] 2.3 新增项目元数据更新接口，至少支持项目标题重命名。
- [x] 2.4 扩展项目持久化 service 和 Supabase schema，使项目标题等管理元数据可被稳定读取和更新。
- [x] 2.5 保持项目管理接口在 Supabase 不可用时返回可恢复错误，不影响现有聊天和生成路由。

## 3. 前端项目目录与切换逻辑

- [x] 3.1 在 `apps/web/src/persistence` 或等价目录中增加项目摘要客户端和本地最近项目索引。
- [x] 3.2 在应用启动流程中补充项目管理初始化逻辑，使当前项目加载与最近项目索引保持同步。
- [x] 3.3 新增“新建画布”动作：创建新项目、更新 URL、进入空白项目，并避免覆写当前项目。
- [x] 3.4 新增“切换画布”动作：根据目标 `projectId` 加载项目，并重置选择、历史栈和活动层级上下文等运行时状态。
- [x] 3.5 明确区分“当前项目远端水合”和“跨项目切换”，避免复用同一状态替换语义。

## 4. 前端入口与基础体验

- [x] 4.1 更新 `WorkspaceHeader` 或等价入口，展示当前画布标题，并提供画布切换器或最近画布入口。
- [x] 4.2 在 UI 中提供“新建画布”入口，并为未命名画布提供默认标题展示。
- [x] 4.3 提供基础重命名能力，使用户可以修改画布标题，而不依赖聊天标题自动命名。
- [x] 4.4 校准空态、加载态和后端降级提示，使无后端场景仍可继续本地画布管理。

## 5. 验证

- [x] 5.1 为 `apps/agent-api` 增加项目列表、创建、重命名和错误降级的单元测试。
- [x] 5.2 为 `apps/web` 增加项目摘要状态、本地最近项目索引、新建画布和切换画布的单元测试。
- [x] 5.3 增加工作区级测试，验证跨项目切换会正确重置选择状态、导航上下文和撤销重做历史。
- [x] 5.4 增加 E2E，覆盖“新建画布 -> 编辑 -> 切换到另一画布 -> 再切回”的完整路径。
- [x] 5.5 运行 `pnpm test`、`pnpm lint` 和 `pnpm build`，确认画布管理能力未破坏现有单项目编辑流程。
